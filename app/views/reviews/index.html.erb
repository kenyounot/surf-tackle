<div class="container">
    <div class="text-center">
        <h1>All Reviews</h1>
        <ul>
            <% @reviews.each do |review| %>
                <li><%= review.title %> - <%= review.stars %></li>
            <% end %>
        </ul>
    </div>
</div>
